# Pin numpy < 2 because tensorflow-macos 2.16 was built against the 1.x
# C-ABI (umath) and will fail to import under numpy 2.x.
numpy>=1.26,<2
scipy
sounddevice
soundfile
bleak
fastapi
uvicorn[standard]
# YAMNet-based snore detector. Required (no fallback). On Apple Silicon
# you also want tensorflow-metal for GPU acceleration.
tensorflow-macos>=2.15,<2.17 ; platform_system == "Darwin"
tensorflow-metal           ; platform_system == "Darwin"
# Standalone Keras 2 — required by the vendored YAMNet code path on
# tensorflow >= 2.16 (which ships Keras 3 by default).
tf_keras
